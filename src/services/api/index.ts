import * as auth from './auth'
import * as profile from './profile'
import * as transport_orders from './transport_orders'
import * as transport_order_info from './transport_order_info'

const API = {
  auth,
  profile,
  transport_orders,
  transport_order_info
}

export default API
